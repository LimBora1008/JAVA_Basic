package class25.exam01.package2;

import class25.exam01.package1.B;

public class C {
	
	// 필드 선언
	// A a; << A클래스(default 클래스 = 같은패키지) C는 다른 패키지 이므로 접근 불가
	B b; // << B클래스(public 클래스 = 모든곳) 접근 가능

}
